# 管理后台 · 静态 HTML 版（当前）

源码目录：**`website/static-test/manage/`**（与官网同项目，多页 HTML + `js/api.js`）。

## 已替代

- ~~`server/admin-web`~~（已删除）
- ~~`server/public/admin/`~~（不再由 Express 托管）
- ~~`website/admin`~~（Vue 版，已移除）

## 部署

见 **[static-test/manage/README.md](../static-test/manage/README.md)**、**[static-test/README.md](../static-test/README.md)**。

**推荐：** 上传整个 `website/static-test/` 到 www，访问：

`https://www.jiankalka.cn/manage/login.html`

## API

不变，仍使用 `server` 的 `/api/v1/admin/*` 与 `POST /api/v1/auth/admin-login`。
