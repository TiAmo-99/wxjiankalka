# 在线工具箱

部署路径：`/tools/`，与官网同目录上传。

| 页面 | 功能 |
|------|------|
| `qrcode.html` | 单条/批量二维码生成、PNG 下载 |
| `json.html` | JSON 校验、格式化、表格互转 |
| `convert.html` | 字符串 ↔ HEX、长度计算 |

入口：官网顶栏 **工具箱** 下拉。

二维码页依赖 jsDelivr 加载 `qrcode` 库（仅浏览器本地生成，不上传服务器）。
